import React from "react";
import Badge from "@/components/Badge";

const Inicio: React.FC = () => {
    return (
        <div>
            <h1>Início</h1>
        </div>
    );
};

export default Inicio;
